<!-- NavBar -->
<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="http://www.chicagotribune.com/sports/baseball/cubs/">
    <img src="./assets/images/tribune.png" width="40" height="40" alt="">
  </a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item active">
        <a class="nav-link" href="/">Home <span class="sr-only">(current)</span></a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/drop">Drop</a>
      </li>
      <li class="nav-item">
          <a href="/scrape" class="btn btn-primary scrape-btn">Scrape!</a>
      </li>
    </ul>
  </div>
</nav>
{{!-- Main Carousel Image --}}
  <div id="carouselExampleIndicators" class="carousel slide" data-ride="carousel">
  <div class="carousel-inner">
    <div class="carousel-item active">
      <img class="d-block w-100" src="./assets/images/madden.jpg" alt="First slide">
        <div class="carousel-caption d-none d-md-block">
        <h5>Talk of the Town!</h5>
        <h6>Check out and chat on the latest headlines from your favorite MLB team thanks to the Chicago Tribune.</h6>
        </div>
    </div><br>
    </div>

{{!-- DIV to create card for each article stored --}}

<ul>
  {{#each found}}
<div class="card">
  <img class="card-img-top" src="{{this.image}}" />
  <h4 class="card-header"><span class="label">{{this.headline}}</span></h4>
  <div class="card-body">
    <p class="card-text"><span class="label"></span> {{this.summary}}</p>
    <a href={{this.link}} class="btn btn-primary">Read Full Article</a>
    <button id="myBtn" data-id={{this._id}} class="btn btn-primary chat-button">Chat!</button>
    <button type="button" href="/delete" data-id={{this._id}} class="btn btn-primary delete-button">Delete</button>

<!-- The Modal -->
<div id="myModal" class="modal">
  <div class="modal-content">
    <span class="close">&times;</span>
    <h4 id="modal-headline"></h4>
    Share your thoughts w/ Chicago!
    <form>
      First name:<br>
      <input type="text" name="firstname"><br>
      Comment:<br>

      <textarea name="comment" rows="10" columns="2"></textarea>
      </form>
    <button type="button" href="/delete" data-id={{this._id}} class="btn btn-primary delete-button">Send</button>
  </div>
</div>
  </div>
<br>
  {{/each}}
  </div>
</ul>



<script>
// Get the modal
var modal = document.getElementById('myModal');

// Get the button that opens the modal
var btn = document.getElementById("myBtn");

// Get the <span> element that closes the modal
var span = document.getElementsByClassName("close")[0];

// When the user clicks on the button, open the modal 
btn.onclick = function() {
    modal.style.display = "block";
}

// When the user clicks on <span> (x), close the modal
span.onclick = function() {
    modal.style.display = "none";
}

// When the user clicks anywhere outside of the modal, close it
window.onclick = function(event) {
    if (event.target == modal) {
        modal.style.display = "none";
    }
}

 $(".chat-button").click(function(event) {
         var modal = document.getElementById('myModal');
          modal.style.display = "block";
    event.preventDefault();
        var chatId = $(this).attr("data-id");
        $.ajax("/comments", {
          type: "POST",
          data: { chatId: chatId }
              }).then(function (response) {
              $('#modal-headline').html(response[0].headline);
              var modal = document.getElementById('myModal');
              modal.style.display = "block";
              console.log(JSON.stringify(response));
    }).catch(function (error) {
        console.log("Something's not working");
    })});

   $(".delete-button").click(function(event) {
        var thisId = $(this).attr("data-id");
        $.ajax("/delete", {
          type: "POST",
          data: { thisId: thisId }
        }).then(
          setTimeout(function() {
            location.reload();
          }, .1000));
        });

    $.ajax("/", {
      type: "GET",
      data: found
    }).then(
      function() {
        {{!-- location.reload(); --}}
      }
    );

 $(".scrape-btn").click(function(event) {
    event.preventDefault();
    $.ajax("/scrape", {
      type: "GET",
      data: found
    }).then(setTimeout(function() {
      res.redirect('/');
      }, .1000)); 
});   
</script>
